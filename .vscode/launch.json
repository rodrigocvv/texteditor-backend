{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch test",
      "program": "${workspaceFolder}/app.js",
      "envFile": "${workspaceFolder}/texteditor.desenv.env",
    }
  ]
}